{
  "name": "kartik-verma-portfolio",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "setup:mediapipe": "node scripts/setup-mediapipe.mjs",
    "check:resume-sync": "node scripts/check-resume-sync.mjs",
    "resume:synced": "node scripts/check-resume-sync.mjs --mark-synced",
    "predev": "npm run setup:mediapipe && npm run check:resume-sync",
    "dev": "astro dev",
    "prebuild": "npm run setup:mediapipe && npm run check:resume-sync",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource-variable/bricolage-grotesque": "^5.3.0",
    "@fontsource-variable/martian-mono": "^5.3.0",
    "@fontsource-variable/onest": "^5.3.1",
    "@mediapipe/tasks-vision": "1.0.1",
    "astro": "^7.3.2",
    "lenis": "^1.3.26",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^22.15.0",
    "typescript": "^5.9.0"
  }
}
